FROM python:3.11-slim

WORKDIR /app

COPY requirements.mcp.txt .
RUN pip install --no-cache-dir -r requirements.mcp.txt

COPY mcp_server.py .

# Force Railway to use the PORT env var
CMD ["sh", "-c", "uvicorn mcp_server:create_app --host 0.0.0.0 --port ${PORT:-8080} --factory"]